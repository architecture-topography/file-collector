version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.12
    steps:
      - checkout
      - run:
          name: "Install requirements"
          command: |
            yarn install
      - run:
          name: "Run Unit Tests"
          command: |
            yarn test
